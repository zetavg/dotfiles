abbr "g"="git %"
abbr "git acp"="git add --all && git commit -m \"wip%\" || : && git push"
abbr "git as"="git add --all && git stash push -m \"%\""
abbr "git cm"="git commit -m \"%\""
abbr "git sa"="git stash apply stash^{/\"%\"}"
abbr "git sp"="git stash push -m \"%\""
abbr "git ss"="git stash"
abbr "gss"="git status --short"
abbr "pg8"="ping 8.8.8.8"
abbr "tpc"="tput cnorm"
